const heading = React.createElement('h1', {}, 'Namaste Everyone!');
const heading2 = React.createElement('h2', {}, 'Namaste everybody!');

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(heading);